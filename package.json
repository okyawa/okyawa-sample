{
  "name": "okyawa-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "firebase-tools": "^9.18.0",
    "http-server": "^14.0.0"
  },
  "scripts": {
    "setup": "sh ./setup.sh",
    "build": "sh ./build.sh",
    "deploy": "npm run build && firebase deploy",
    "http-server": "http-server ./ -o",
    "serve": "firebase serve --only hosting --host 0.0.0.0 ",
    "login": "firebase login",
    "logout": "firebase logout",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "okyawa",
  "license": "ISC"
}
